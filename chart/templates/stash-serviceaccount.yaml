{{- if .Values.stash.enabled }}
{{- if .Values.stash.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "media-stack.serviceAccountName" (dict "root" . "app" "stash") }}
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "stash") | nindent 4 }}
  {{- with .Values.stash.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
