{{- if .Values.global.ingress.enabled }}
{{- if .Values.jellyfin.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "jellyfin") }}
  labels:
    {{- include "media-stack.labels" . | nindent 4 }}
  {{- with .Values.global.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.global.ingress.className }}
  ingressClassName: {{ .Values.global.ingress.className }}
  {{- end }}
  {{- if .Values.global.ingress.tls }}
  tls:
    {{- toYaml .Values.global.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    - host: {{ .Values.jellyfin.ingress.subdomain }}.{{ .Values.global.baseDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "media-stack.appFullname" (dict "root" . "app" "jellyfin") }}
                port:
                  number: {{ .Values.jellyfin.service.port }}
{{- end }}
{{- if .Values.plex.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "plex") }}
  labels:
    {{- include "media-stack.labels" . | nindent 4 }}
  {{- with .Values.global.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.global.ingress.className }}
  ingressClassName: {{ .Values.global.ingress.className }}
  {{- end }}
  {{- if .Values.global.ingress.tls }}
  tls:
    {{- toYaml .Values.global.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    - host: {{ .Values.plex.ingress.subdomain }}.{{ .Values.global.baseDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "media-stack.appFullname" (dict "root" . "app" "plex") }}
                port:
                  number: {{ .Values.plex.service.port }}
{{- end }}
{{- if .Values.prowlarr.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "prowlarr") }}
  labels:
    {{- include "media-stack.labels" . | nindent 4 }}
  {{- with .Values.global.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.global.ingress.className }}
  ingressClassName: {{ .Values.global.ingress.className }}
  {{- end }}
  {{- if .Values.global.ingress.tls }}
  tls:
    {{- toYaml .Values.global.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    - host: {{ .Values.prowlarr.ingress.subdomain }}.{{ .Values.global.baseDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "media-stack.appFullname" (dict "root" . "app" "prowlarr") }}
                port:
                  number: {{ .Values.prowlarr.service.port }}
{{- end }}
{{- if .Values.sonarr.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "sonarr") }}
  labels:
    {{- include "media-stack.labels" . | nindent 4 }}
  {{- with .Values.global.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.global.ingress.className }}
  ingressClassName: {{ .Values.global.ingress.className }}
  {{- end }}
  {{- if .Values.global.ingress.tls }}
  tls:
    {{- toYaml .Values.global.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    - host: {{ .Values.sonarr.ingress.subdomain }}.{{ .Values.global.baseDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "media-stack.appFullname" (dict "root" . "app" "sonarr") }}
                port:
                  number: {{ .Values.sonarr.service.port }}
{{- end }}
{{- if .Values.radarr.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "radarr") }}
  labels:
    {{- include "media-stack.labels" . | nindent 4 }}
  {{- with .Values.global.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.global.ingress.className }}
  ingressClassName: {{ .Values.global.ingress.className }}
  {{- end }}
  {{- if .Values.global.ingress.tls }}
  tls:
    {{- toYaml .Values.global.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    - host: {{ .Values.radarr.ingress.subdomain }}.{{ .Values.global.baseDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "media-stack.appFullname" (dict "root" . "app" "radarr") }}
                port:
                  number: {{ .Values.radarr.service.port }}
{{- end }}
{{- if .Values.sabnzbd.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "sabnzbd") }}
  labels:
    {{- include "media-stack.labels" . | nindent 4 }}
  {{- with .Values.global.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.global.ingress.className }}
  ingressClassName: {{ .Values.global.ingress.className }}
  {{- end }}
  {{- if .Values.global.ingress.tls }}
  tls:
    {{- toYaml .Values.global.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    - host: {{ .Values.sabnzbd.ingress.subdomain }}.{{ .Values.global.baseDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "media-stack.appFullname" (dict "root" . "app" "sabnzbd") }}
                port:
                  number: {{ .Values.sabnzbd.service.port }}
{{- end }}
{{- end }}
