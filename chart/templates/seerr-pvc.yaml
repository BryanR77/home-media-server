{{- if .Values.seerr.enabled }}
{{- if .Values.seerr.persistence.config.enabled }}
{{- if not .Values.seerr.persistence.config.existingClaim }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "seerr") }}-config
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "seerr") | nindent 4 }}
    app.kubernetes.io/component: config
  {{- with .Values.seerr.persistence.config.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.seerr.persistence.config.accessMode }}
  {{- if .Values.seerr.persistence.config.storageClass }}
  {{- if (eq "-" .Values.seerr.persistence.config.storageClass) }}
  storageClassName: ""
  {{- else }}
  storageClassName: {{ .Values.seerr.persistence.config.storageClass }}
  {{- end }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.seerr.persistence.config.size }}
{{- end }}
{{- end }}
{{- end }}
