{{- if .Values.radarr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "radarr") }}
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "radarr") | nindent 4 }}
  {{- with .Values.radarr.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.radarr.service.type }}
  ports:
    - port: {{ .Values.radarr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "media-stack.appSelectorLabels" (dict "root" . "app" "radarr") | nindent 4 }}
{{- end }}
