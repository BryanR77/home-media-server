{{- if .Values.tautulli.enabled }}
{{- if .Values.tautulli.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "media-stack.serviceAccountName" (dict "root" . "app" "tautulli") }}
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "tautulli") | nindent 4 }}
  {{- with .Values.tautulli.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
