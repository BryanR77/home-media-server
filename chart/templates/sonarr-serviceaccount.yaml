{{- if .Values.sonarr.enabled }}
{{- if .Values.sonarr.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "media-stack.serviceAccountName" (dict "root" . "app" "sonarr") }}
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "sonarr") | nindent 4 }}
  {{- with .Values.sonarr.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
