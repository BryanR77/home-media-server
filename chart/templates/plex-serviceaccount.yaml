{{- if .Values.plex.enabled }}
{{- if .Values.plex.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "media-stack.serviceAccountName" (dict "root" . "app" "plex") }}
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "plex") | nindent 4 }}
  {{- with .Values.plex.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
---
{{- if .Values.plex.hardwareAcceleration.intelQuickSync.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "plex") }}-scc-role
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "plex") | nindent 4 }}
rules:
  - apiGroups:
      - security.openshift.io
    resources:
      - securitycontextconstraints
    resourceNames:
      - {{ include "media-stack.appFullname" (dict "root" . "app" "plex") }}-quicksync
    verbs:
      - use
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "plex") }}-scc-binding
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "plex") | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "plex") }}-scc-role
subjects:
  - kind: ServiceAccount
    name: {{ include "media-stack.serviceAccountName" (dict "root" . "app" "plex") }}
{{- end }}
{{- end }}
