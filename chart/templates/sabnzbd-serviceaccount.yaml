{{- if .Values.sabnzbd.enabled }}
{{- if .Values.sabnzbd.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "media-stack.serviceAccountName" (dict "root" . "app" "sabnzbd") }}
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "sabnzbd") | nindent 4 }}
  {{- with .Values.sabnzbd.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
