{{- if .Values.radarr.enabled }}
{{- if .Values.radarr.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "media-stack.serviceAccountName" (dict "root" . "app" "radarr") }}
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "radarr") | nindent 4 }}
  {{- with .Values.radarr.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
