{{- if .Values.tdarr.enabled }}
{{- if .Values.tdarr.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "media-stack.serviceAccountName" (dict "root" . "app" "tdarr") }}
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "tdarr") | nindent 4 }}
  {{- with .Values.tdarr.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
