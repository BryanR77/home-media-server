{{- if .Values.seerr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "seerr") }}
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "seerr") | nindent 4 }}
  {{- with .Values.seerr.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.seerr.service.type }}
  ports:
    - port: {{ .Values.seerr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "media-stack.appSelectorLabels" (dict "root" . "app" "seerr") | nindent 4 }}
{{- end }}
