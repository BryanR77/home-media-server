{{- if .Values.plex.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "media-stack.appFullname" (dict "root" . "app" "plex") }}
  labels:
    {{- include "media-stack.appLabels" (dict "root" . "app" "plex") | nindent 4 }}
spec:
  type: {{ .Values.plex.service.type }}
  ports:
    - port: {{ .Values.plex.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "media-stack.appSelectorLabels" (dict "root" . "app" "plex") | nindent 4 }}
{{- end }}
